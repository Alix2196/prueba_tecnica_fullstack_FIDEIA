# Copiar a .env (o exportar como variables de entorno) antes de levantar el backend.
DB_URL=jdbc:postgresql://localhost:5432/tickets
DB_USER=tickets
DB_PASSWORD=tickets
SERVER_PORT=8080
APP_CORS_ALLOWED_ORIGINS=http://localhost:4200
